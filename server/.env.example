# MongoDB Atlas (cloud) — recommended for production and shared data
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/ecocircle?appName=Cluster0

# Local MongoDB Compass (optional fallback when MONGODB_URI is not set)
# LOCAL_MONGO_URI=mongodb://localhost:27017/ecocircle

# PORT=3000
