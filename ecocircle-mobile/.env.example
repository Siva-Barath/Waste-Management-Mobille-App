# API URL — auto-detected in Expo Go from your Metro host.
# Set manually if needed (replace with your PC's LAN IP for physical devices):
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api

# Android emulator fallback: http://10.0.2.2:5000/api
# iOS simulator fallback: http://localhost:5000/api
